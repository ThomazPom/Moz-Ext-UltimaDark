{
  "dependencies": {
    "alpinejs": "^3.14.8",
    "bootstrap": "^5.3.3",
    "esbuild": "^0.25.0"
  },
  "scripts": {
    "watch": "npx esbuild popup.js --bundle --sourcemap --outfile=bundle.js --loader:.css=css --watch",
    "build": "npx esbuild popup.js --bundle --sourcemap --outfile=bundle.js --loader:.css=css"
  }
  
}
